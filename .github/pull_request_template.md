## Summary

- What changed?
- Why is this needed?

## Checklist

- [ ] Tests added/updated if needed
- [ ] `PYTHONPATH=src python -m pytest -q` passed
- [ ] `PYTHONPATH=src python scripts/run_daily.py` runs without crash
- [ ] Docs/config updated (`README.md`, `config/sources.yaml`) if behavior changed
- [ ] No secrets committed

## Validation

Provide key command outputs or screenshots if helpful.
